## A sample spring boot app with spring-jdbc-template approach

### To run commandline version:

from terminal: `./gradlew bootRun` from IDE also bootRun

Check the application logs to see what happened
